<%= render partial: "shared/menu"%>
<h1>Welcome Meme Friends! <h1>
<h2> R.i.p Stefan Karl Stefansson</h2>
<%# Only found way of presenting data as envisioned required multiple calls, plz show screen shots of better code with less call back that works the same %> 
<% @memes.reverse.each do |m| %>
       
    <h4>Name:<%= m.name %></h4></br>
    <%= image_tag m.image if m.image.attached? %>
    <h6><%= link_to "#{m.name}'s page", user_meme_path(m[:user_id], m.id) %></h6></br>
    <h6><%= link_to "#{User.find(m.user_id).username}'s Page", user_path(m.user_id) %></h6></br>
      
<% end %>
